Burpsuite#Target, Proxy, Intruder
List open ports on Linux machine#netstat tulpn
Runtime Linker#/lib/x86_64-linux-gnu/ld-2.28.so
C++ name mangling#Translates C++ names to C names -> makes them unique by e.g. mangling in types
stringstream#>> Extracts formated data, << Inserts formated data, str() to get content and str(string) to write content
Vector#{Array, that, can} change in size -> more capacity than what is strictly needed (relocating all elements is expensive)
Simple Object Access Protocol (SOAP)#Austausch von XML formatierten Daten in Anwendungs Layer, Envelope - Header - Body
